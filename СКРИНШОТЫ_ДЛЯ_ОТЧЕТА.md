# Где брать скриншоты для отчёта (начиная с рисунка 41)

## 5.3 Nominatim API

### Запрос 1: Прямой геокодинг (geocodeAddress)

**Рисунок 41** - Реализация метода в data source:
- Файл: `lib/data/datasources/remote/nominatim/nominatim_remote_data_source.dart`
- Строки: 36-55 (метод `geocodeAddress`)

**Рисунок 42** - Реализация взаимодействия с API:
- Файл: `lib/data/datasources/remote/nominatim/nominatim_remote_data_source.dart`
- Строки: 36-55 (весь метод `geocodeAddress`)

**Рисунок 43** - Use case:
- Файл: `lib/domain/usecases/geocoding/geocode_address_usecase.dart`
- Весь файл (строки 1-20)

**Рисунок 44** - Демонстрация работы в приложении:
- Экран: "Добавить объявление" (`add_item_screen.dart`)
- Действие: Введите адрес (например, "Москва, Красная площадь") и нажмите "Сохранить"
- Скриншот: Экран создания объявления с заполненным полем адреса и кнопкой сохранения

---

### Запрос 2: Поиск адресов (searchAddresses)

**Рисунок 45** - Реализация метода в data source:
- Файл: `lib/data/datasources/remote/nominatim/nominatim_remote_data_source.dart`
- Строки: 99-128 (метод `searchAddresses`)

**Рисунок 46** - Реализация взаимодействия с API:
- Файл: `lib/data/datasources/remote/nominatim/nominatim_remote_data_source.dart`
- Строки: 99-128 (весь метод `searchAddresses`)

**Рисунок 47** - Use case:
- Файл: `lib/domain/usecases/geocoding/с.dart`
- Весь файл (строки 1-19)

**Рисунок 48** - Демонстрация работы в приложении:
- Экран: "Добавить объявление" (`add_item_screen.dart`)
- Действие: Начните вводить адрес в поле "Адрес" (например, "Москва")
- Скриншот: Экран с полем адреса и списком подсказок под полем (автодополнение)

---

### Запрос 3: Поиск адресов в городе (searchAddressesInCity)

**Рисунок 49** - Реализация метода в data source:
- Файл: `lib/data/datasources/remote/nominatim/nominatim_remote_data_source.dart`
- Строки: 130-163 (метод `searchAddressesInCity`)

**Рисунок 50** - Реализация взаимодействия с API:
- Файл: `lib/data/datasources/remote/nominatim/nominatim_remote_data_source.dart`
- Строки: 130-163 (весь метод `searchAddressesInCity`)

**Рисунок 51** - Use case:
- Файл: `lib/domain/usecases/geocoding/search_addresses_in_city_usecase.dart`
- Весь файл (строки 1-19)

**Рисунок 52** - Демонстрация работы в приложении:
- Экран: "Добавить объявление" (`add_item_screen.dart`)
- Действие: Включите чекбокс "Искать адреса в конкретном городе", введите запрос (например, "улица") и город (например, "Москва")
- Скриншот: Экран с включённым чекбоксом, заполненными полями и списком результатов под полем

---

### Запрос 4: Поиск адресов в стране (searchAddressesByCountry)

**Рисунок 53** - Реализация метода в data source:
- Файл: `lib/data/datasources/remote/nominatim/nominatim_remote_data_source.dart`
- Строки: 64-97 (метод `searchAddressesByCountry`)

**Рисунок 54** - Реализация взаимодействия с API:
- Файл: `lib/data/datasources/remote/nominatim/nominatim_remote_data_source.dart`
- Строки: 64-97 (весь метод `searchAddressesByCountry`)

**Рисунок 55** - Use case:
- Файл: `lib/domain/usecases/geocoding/search_addresses_by_country_usecase.dart`
- Весь файл (строки 1-17)

**Рисунок 56** - Демонстрация работы в приложении:
- Экран: Главный экран с объявлениями (`listings_screen.dart`)
- Действие: Нажмите кнопку "Адреса" в навигации → откроется диалог → введите запрос (например, "улица") и страну (например, "Россия") → нажмите "Найти"
- Скриншот: Диалоговое окно "Поиск адресов в стране" с заполненными полями и списком найденных адресов

---

### Запрос 5: Поиск объектов по типу (searchObjectsByType)

**Рисунок 57** - Реализация метода в data source:
- Файл: `lib/data/datasources/remote/nominatim/nominatim_remote_data_source.dart`
- Строки: 165-216 (метод `searchObjectsByType`)

**Рисунок 58** - Реализация взаимодействия с API:
- Файл: `lib/data/datasources/remote/nominatim/nominatim_remote_data_source.dart`
- Строки: 165-216 (весь метод `searchObjectsByType`)

**Рисунок 59** - Use case:
- Файл: `lib/domain/usecases/geocoding/search_objects_by_type_usecase.dart`
- Весь файл (строки 1-18)

**Рисунок 60** - Демонстрация работы в приложении:
- Экран: Главный экран с объявлениями (`listings_screen.dart`)
- Действие: Нажмите кнопку "Пункты приема" в навигации → откроется диалог → введите тип объекта (например, "переработка") и город (например, "Москва") → нажмите "Найти"
- Скриншот: Диалоговое окно "Поиск пунктов приема" с заполненными полями и списком найденных объектов

---

## 5.4 Wikipedia API

### Запрос 1: Поиск статей (searchArticles)

**Рисунок 61** - Реализация метода в data source:
- Файл: `lib/data/datasources/remote/wikipedia/wikipedia_remote_data_source.dart`
- Строки: 40-72 (метод `searchArticles`)

**Рисунок 62** - Реализация взаимодействия с API:
- Файл: `lib/data/datasources/remote/wikipedia/wikipedia_remote_data_source.dart`
- Строки: 40-72 (весь метод `searchArticles`)

**Рисунок 63** - Use case:
- Файл: `lib/domain/usecases/wikipedia/search_wikipedia_articles_usecase.dart`
- Весь файл (строки 1-17)

**Рисунок 64** - Демонстрация работы в приложении:
- Экран: "Эко-гид" (`eco_tips_screen.dart`)
- Действие: Введите поисковый запрос в поле поиска (например, "переработка отходов") и нажмите "Найти"
- Скриншот: Экран "Эко-гид" с полем поиска, введённым запросом и списком найденных статей в виде карточек

---

### Запрос 2: Получение содержимого статьи (getPageContent)

**Рисунок 65** - Реализация метода в data source:
- Файл: `lib/data/datasources/remote/wikipedia/wikipedia_remote_data_source.dart`
- Строки: 74-103 (метод `getPageContent`)

**Рисунок 66** - Реализация взаимодействия с API:
- Файл: `lib/data/datasources/remote/wikipedia/wikipedia_remote_data_source.dart`
- Строки: 74-103 (весь метод `getPageContent`)

**Рисунок 67** - Use case:
- Файл: `lib/domain/usecases/wikipedia/get_wikipedia_page_content_usecase.dart`
- Весь файл (строки 1-17)

**Рисунок 68** - Демонстрация работы в приложении:
- Экран: "Эко-гид" (`eco_tips_screen.dart`)
- Действие: Нажмите на карточку статьи в списке результатов поиска
- Скриншот: Экран с открытой статьёй, показывающий полное содержимое статьи с текстом и изображением

---

### Запрос 3: Получение статей из категории (getCategoryMembers)

**Рисунок 69** - Реализация метода в data source:
- Файл: `lib/data/datasources/remote/wikipedia/wikipedia_remote_data_source.dart`
- Строки: 105-142 (метод `getCategoryMembers`)

**Рисунок 70** - Реализация взаимодействия с API:
- Файл: `lib/data/datasources/remote/wikipedia/wikipedia_remote_data_source.dart`
- Строки: 105-142 (весь метод `getCategoryMembers`)

**Рисунок 71** - Use case:
- Файл: `lib/domain/usecases/wikipedia/get_wikipedia_category_articles_usecase.dart`
- Весь файл (строки 1-17)

**Рисунок 72** - Демонстрация работы в приложении:
- Экран: Детали объявления (`item_detail_screen.dart`)
- Действие: Откройте любое объявление → раскройте секцию "Информация об экологии"
- Скриншот: Экран деталей объявления с раскрытой секцией "Информация об экологии" и списком статей из категории, соответствующей категории товара

---

### Запрос 4: Получение связанных статей (getPageLinks)

**Рисунок 73** - Реализация метода в data source:
- Файл: `lib/data/datasources/remote/wikipedia/wikipedia_remote_data_source.dart`
- Строки: 144-183 (метод `getPageLinks`)

**Рисунок 74** - Реализация взаимодействия с API:
- Файл: `lib/data/datasources/remote/wikipedia/wikipedia_remote_data_source.dart`
- Строки: 144-183 (весь метод `getPageLinks`)

**Рисунок 75** - Use case:
- Файл: `lib/domain/usecases/wikipedia/get_wikipedia_page_links_usecase.dart`
- Весь файл (строки 1-17)

**Рисунок 76** - Демонстрация работы в приложении:
- Экран: "Эко-гид" (`eco_tips_screen.dart`) или "Детали объявления" (`item_detail_screen.dart`)
- Действие: Нажмите кнопку с иконкой ссылки (🔗) на любой карточке статьи
- Скриншот: Экран с результатами поиска, показывающий список связанных статей (заменившие предыдущие результаты)

---

### Запрос 5: Получение изображений статьи (getPageImages)

**Рисунок 77** - Реализация метода в data source:
- Файл: `lib/data/datasources/remote/wikipedia/wikipedia_remote_data_source.dart`
- Строки: 185-252 (метод `getPageImages`)

**Рисунок 78** - Реализация взаимодействия с API:
- Файл: `lib/data/datasources/remote/wikipedia/wikipedia_remote_data_source.dart`
- Строки: 185-252 (весь метод `getPageImages`)

**Рисунок 79** - Use case:
- Файл: `lib/domain/usecases/wikipedia/get_wikipedia_page_images_usecase.dart`
- Весь файл (строки 1-17)

**Рисунок 80** - Демонстрация работы в приложении:
- Экран: "Руководство по переработке" (`recycling_guide_screen.dart` в `eco_tips_screen.dart`)
- Действие: Выберите категорию отходов (например, "Электроника")
- Скриншот: Экран "Руководство по переработке" с выбранной категорией, показывающий горизонтальный прокручиваемый список изображений из Wikipedia над описанием шагов по переработке

---

## Итого: 40 скриншотов (рисунки 41-80)

### Скриншоты кода (30 штук):
- 10 методов data source (Nominatim: 5, Wikipedia: 5)
- 10 use cases (Nominatim: 5, Wikipedia: 5)
- 10 повторных скриншотов data source (для "взаимодействия с API")

### Скриншоты UI (10 штук):
- 5 демонстраций работы Nominatim API
- 5 демонстраций работы Wikipedia API


